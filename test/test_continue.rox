function main() -> none {
    list[int64] lst;
    for i in range(0, 5, 1) {
        rox_result[int64] g = i%2;
        if(isOk(g)) {
            if(getValue(g) == 0) {
                continue;
            }
            lst.append(i);
        } else {
            print("Error: Failed to calculate modulo for ", i , "/2 \n");
            return;
        }
    }
    if (not (lst.size() == 2)) {
        print("Error: Expected size 2, got ", lst.size(), "\n");
        return;
    }
    for i in range(0, lst.size(), 1) {
        rox_result[int64] got = lst.at(i);
        if(isOk(got)) {
            if (not (getValue(got) == (i*2+1))) {
                print("Error: Expected ", (i*2+1), ", got ", getValue(got), "\n");
                return;
            }
        } else {
            print("failed to get item at index: ", i, "\n");
            return;
        }
    }
    print("Done!");

}