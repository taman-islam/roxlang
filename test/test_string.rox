function main() -> none {
    string s = "Hello";

    // Check size
    if (not (s.size() == 5)) {
        print("Size Fail\n");
        return none;
    }

    // Check at
    rox_result[char] r = s.at(1);

    if (isOk(r)) {
         if (not (getValue(r) == 'e')) {
             print("At Fail 2\n");
             return none;
         }
    } else {
         print("At Fail 1\n");
         return none;
    }

    // Check equality
    if (s == "Hello") {
        // ok
    } else {
        print("Eq Fail\n");
        return none;
    }

    // Direct print
    print(s);
    print("\n");
    print("Variadic", " ", "Print", " ", "Works\n");
    print("Passed\n");
}
