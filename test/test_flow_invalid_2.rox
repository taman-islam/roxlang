function main() -> none {
    list[int64] int64s = [1, 2, 3];
    rox_result[int64] res = int64s.at(0);

    if (isOk(res)) {
        // Safe here
        int64 v1 = getValue(res);

        // Invalidate by reassignment
        res = int64s.at(1);

        // Unsafe access: res is no longer proven safe
        int64 v2 = getValue(res);
    }
}
